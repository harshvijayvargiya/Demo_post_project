
<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="header">
      <%= link_to "Demo Post", '#', id: "logo" %>
      <nav>
        <ul class="nav pull-right">
         <% if user_signed_in? %>
         <form class="navbar-form navbar-left" action="#">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="Search" name="search">
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form>
        <%= link_to 'Home',root_url, class:'btn btn-primary'%>
        <a class="dropdown-toggle" data-toggle="dropdown"> <%= image_tag current_user.profile_picture.url, id: "image_profile"%>
          <ul class="dropdown-menu">
            <li><%= link_to "#{current_user.first_name}'s Post", posts_path, class: 'btn' %></li>
            <li> <%= link_to 'Edit Profile', edit_user_registration_path, class: 'btn' %></li>
            <li class="dropdown"></li>
            <li><%= link_to('Logout', destroy_user_session_path, class: 'btn', method: :delete) %></li> <%# destroy_user_session_path option would go here as well %>
          </ul>

          <% else %>
          <div class="btn-group">
           <%= link_to "About",  new_post_path, class: 'btn btn-info' %>
           <%= link_to "Contact", new_user_registration_path , class: 'btn btn-info'%>

           <%= link_to('Login', new_user_session_path, class: 'btn btn-info')  %> 
           <%= link_to('Signup', new_user_registration_path, class: 'btn btn-info')  %>
         </div>
         <% end %>

       </ul>
     </nav>
   </div>
 </div>
</header>
